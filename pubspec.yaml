name: concha
description: "Concha — hear it again. sing it better."
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 0.0.1

environment:
  sdk: ^3.10.8
dependencies:
  flutter:
    sdk: flutter

  styled_widget: ^0.4.1
  path_provider: ^2.1.5
  file_selector: ^1.1.0
  animations: ^2.1.1
  json_annotation: ^4.11.0
  uuid: ^4.5.1
  provider: ^6.1.5
  nested: ^1.0.0
  audio_metadata_reader: ^1.4.2
  flutter_soloud: ^3.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.10.3
  json_serializable: ^6.11.1

flutter:
  uses-material-design: true
